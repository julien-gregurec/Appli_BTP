# Registre central ELSATIA

Dernière consolidation : 7 août 2026 (P2 — bootstrap Supabase Production par migrations, après P1, le mini-lot garde-fou des scripts de recette Supabase, le lot documentaire B1-B2, le lot technique pré-commercialisation B3-B5 et la clôture définitive de la recette d'isolation multi-entreprises).

## Projet de référence

Le projet de référence est :

`/Users/juliengregurec/Documents/btp-platform`

Les travaux de commercialisation sont réalisés dans le worktree Git dédié :

`/Users/juliengregurec/Projects/liria-codex`

Le dépôt principal reste la source de référence. Le worktree ne constitue pas un second projet et ne doit pas être mélangé avec un autre worktree.

## État des lots de commercialisation

La numérotation ci-dessous est la numérotation actuelle et fait foi. Les anciens plans ou rapports historiques peuvent employer une autre numérotation ; ils ne doivent pas être réécrits.

| Lot | Périmètre | État | Référence |
| --- | --- | --- | --- |
| 1 | Audit et plan de renommage | Validé et intégré | Historique de `release/commercialisation-v1` |
| 2 | Configuration centralisée de marque | Validé et intégré | Historique de `release/commercialisation-v1` |
| 3 | Interface, PWA, métadonnées, documents et e-mails | Validé et intégré | Historique de `release/commercialisation-v1` |
| 4 | Assistant IA | Validé et intégré | `1c4d1b8a99f7dfb1502af7539f70d65463c4d252` |
| 5 | Migration 194 et données actives | Validé et intégré | `aea62ff02d7711df049714cd2d4b34bb8496a8b9` |
| 6 | Nettoyage final de l’ancienne identité | Terminé et validé localement sur `lot6/nettoyage-final-elsatia` | Aucun push ni déploiement |
| 7 | Garde-fou automatisé contre le retour de l’ancien nom | Non commencé | À autoriser séparément après validation du lot 6 |

## Règles d’identité

- Marque, éditeur et logo texte : **ELSATIA**.
- Logiciel, interface, PWA, PDF, e-mails et assistant IA : **ELSATIA Gestion Pro**.
- Fournisseur automatique de la boutique : **ELSATIA (boutique)**.
- Aucun domaine officiel ne doit être inventé ou codé en dur ; utiliser la configuration d’environnement.
- Les migrations historiques, audits historiques, rapports de relais et médias archivés restent inchangés.

## État des autres sujets

| Sujet | État au 5 août 2026 | Emplacement ou prochaine action |
| --- | --- | --- |
| Application ELSATIA Gestion Pro | Projet principal actif | Racine du dépôt |
| Recette fonctionnelle Preview (R1-R7C) | Prestations, Fournisseurs, Clients et Devis validés en conditions réelles sur `elsatia-preview` | Suite prévue : authentification, isolation multi-entreprises, Storage privé, Factures, Stripe test, Mobile/PWA — voir `docs/organisation/CHECKLIST_LANCEMENT.md` |
| Recette Auth (inscription, confirmation, connexion, déconnexion, mot de passe oublié) | Phases 0 à 4 validées sur `elsatia-preview` (commit `06f8b6e`) ; Phase 5 « mot de passe oublié » en pause (quota d'envoi d'e-mails Supabase atteint) | Reprise de la Phase 5 uniquement, profil vierge, une seule demande de lien, clic unique en navigation privée |
| Recette Isolation Multi-Entreprises — vérification préalable | **PHASE 0 VALIDÉE** (5 août 2026). Projet Vercel `elsatia-preview` confirmé (résolution CLI `julien-gregurec1/elsatia-preview`) ; `SUPABASE_SERVICE_ROLE_KEY` et `NEXT_PUBLIC_SUPABASE_URL` existent chacune une seule fois, scopées Preview uniquement, aucun doublon, aucune trace du projet parasite `liria-concept-gestion-btp` (lié par erreur au répertoire personnel, cause d'une fausse anomalie entre-temps résolue) ; projet Supabase `elsatia-preview`, réf. `pgvvpqyjziyapbbkydmc`, région `eu-west-3`/Paris (UE). Remplacement de la valeur de `SUPABASE_SERVICE_ROLE_KEY` à partir du projet Supabase `elsatia-preview` confirmé manuellement par l'utilisateur (valeur jamais affichée, copiée ni enregistrée) | Voir ligne « Provisionnement entreprise B » ci-dessous pour la suite |
| Recette Isolation Multi-Entreprises — provisionnement entreprise B | Compte B (`recette.isolation.b@elsatia-preview.invalid`) et entreprise B (`958398eb-0e0e-4123-be8e-d13d384d1fdb`, `RECETTE_ISOLATION_B — Entreprise Fictive`) créés via les parcours normaux de l'application. **Bug bloquant découvert puis corrigé le 6 août 2026** : `public.chantiers` n'avait plus aucune policy RLS PERMISSIVE couvrant INSERT/UPDATE/DELETE depuis la migration `20260715000081` — empêchait la création de tout chantier via l'UI, sur n'importe quelle entreprise, depuis le 15 juillet 2026. Corrigé par la migration `20260806000196_correctif_rls_ecriture_chantiers.sql`, validée (15 tests pgTAP, 232 Vitest, typecheck, lint, build), appliquée sur `elsatia-preview`, intégrée en fast-forward sur `release/commercialisation-v1` (commit `5161bf5`). Provisionnement complet : client `RECETTE_ISOLATION_B_CLIENT_1` (`CLI-0001`), chantier `RECETTE_ISOLATION_B_CHANTIER_1` (`CHA-2026-001`), salarié `RECETTE_ISOLATION_B_EMPLOYE_1`, fournisseur `RECETTE_ISOLATION_B_FOURNISSEUR_1` (`FRN-0001`), devis `RECETTE_ISOLATION_B_DEVIS_1` (`DEV-2026-001`, Accepté, HT 1 000,00 € / TVA 150,00 € / TTC 1 150,00 €), facture `RECETTE_ISOLATION_B_FACTURE_1` (transformée depuis le devis, Brouillon, mêmes montants), dépense `RECETTE_ISOLATION_B_DEPENSE_1` (HT 100,00 € / TVA 20,00 € / TTC 120,00 €) | Voir ligne « Recette d'isolation en lecture A↔B » ci-dessous |
| Recette Isolation Multi-Entreprises — recette en lecture A↔B | **VALIDÉE le 6 août 2026, sans anomalie.** Catégories testées dans les deux sens : listes (clients, chantiers, salariés, fournisseurs, devis, factures, dépenses), tableaux de bord et compteurs, sélecteurs de formulaire, routes directes par UUID, requêtes API/RLS simulées (rôle `authenticated`, JWT réel de chaque compte, transactions systématiquement annulées), fonctions RPC de lecture (`est_membre_actif`, `a_permission`, `peut_consulter_chantier`) appelées avec des paramètres croisés. Résultat A→B et B→A : toutes les recherches directes, filtres par `entreprise_id` et marqueurs `RECETTE_ISOLATION_B_` à `0` ligne ; comptages sans filtre strictement égaux aux totaux propres de chaque entreprise ; 7 routes directes vers des UUID de l'autre entreprise → `404` systématique dans les deux sens ; RPC croisées → toutes `false`. Point de vigilance documenté : les références courtes (`FRN-0001`, `DEV-2026-001`) existent en parallèle dans A et B par coïncidence de numérotation propre à chaque entreprise (confirmé en vérifiant l'identité réelle de chaque ressource, pas seulement la référence) — ce n'est pas une fuite. **Storage** : aucun objet B n'existe (aucune pièce jointe créée durant le provisionnement) — test fonctionnel reporté. Baselines A et B strictement inchangées avant/après toute la recette | Voir ligne « Recette d'écriture croisée — IDOR factures » ci-dessous |
| Recette Isolation Multi-Entreprises — recette d'écriture croisée (INSERT/UPDATE/DELETE, IDOR, Storage destructif, RPC offensif) | **Suspendue le 6 août 2026** après confirmation d'une fuite réelle sur `public.factures` (voir ligne suivante). Périmètre déjà couvert avant suspension : INSERT rollback sur 9 tables × 5 combinaisons de désaccord d'entreprise. Non commencés : UPDATE, DELETE, Server Actions, RPC offensif, Storage destructif | **À reprendre séparément**, après autorisation explicite distincte de celle du correctif `factures` |
| Recette d'écriture croisée — IDOR `factures` (découverte, correctif, validation) | **Fuite confirmée le 6 août 2026** : `public.factures` acceptait, via INSERT direct (hors flux applicatifs), une valeur de `client_id`, `devis_origine_id`, `facture_origine_id` ou `facture_parente_id` appartenant à une autre entreprise que la facture elle-même — seule `chantier_id` était déjà protégée par une clé étrangère composite (`factures_chantier_entreprise_fkey`). Root cause : les policies RLS de `factures` ne contrôlent que `factures.entreprise_id`, jamais la cohérence des relations sortantes ; ce n'est pas une régression, la faille existait avant le début de la recette. Exploitabilité circonscrite : uniquement via appel direct à l'API REST/SQL Supabase — non atteignable depuis l'UI ni les Server Actions (`src/app/actions/factures.ts` ne fait aucun `.insert()` brut sur `factures`), ni depuis les RPC `creer_facture_depuis_devis`/`modifier_facture_brouillon` (toutes deux `security invoker`, re-lisent la ressource source sous la RLS de l'appelant, donc une référence étrangère y provoque un « introuvable » applicatif, pas une fuite). Aucune donnée résiduelle : chaque test a été exécuté puis annulé (`rollback`), aucune ligne `RECETTE_IDOR_*` n'a persisté. **Correctif appliqué et validé** : migration `20260806000197_correctif_isolation_factures.sql` (ajout de clés étrangères composites `(colonne, entreprise_id) → (id, entreprise_id)` sur les 4 relations, plus les 2 index uniques manquants sur `devis` et `factures` ; migration additive, aucune policy RLS modifiée). Validation locale : 28 assertions pgTAP dédiées + 212 tests pgTAP au total, 232 tests Vitest, `typecheck`, `lint`, `build` — tous au vert. Contrôle de cohérence des données réelles sur `elsatia-preview` avant application : 0 incohérence sur les 4 relations. Migration appliquée sur `elsatia-preview` (`migration list` local=remote), état vivant vérifié (5 contraintes dont la nouvelle et la préexistante, 2 index uniques, 5 policies RLS inchangées). 10 tests directs sur `elsatia-preview` (transactions annulées, comptes réels A/B) : les 6 opérations auparavant permissives sont désormais bloquées (`23503`, violation de la nouvelle clé étrangère composite) dans les deux sens A→B et B→A ; les 4 opérations légitimes (mêmes colonnes que la RPC de transformation, y compris facture manuelle sans devis) restent possibles. Après rollback : baselines reconfirmées à l'identique (factures A = 25, factures B = 1, 0 ligne résiduelle). Compatibilité applicative : établie par analyse de code (RPC `security invoker`, valeurs dérivées de la ressource source de la même entreprise) et par les assertions de non-régression pgTAP reproduisant exactement les colonnes des RPC ; aucun appel RPC live supplémentaire n'a été effectué sur Preview faute de devis « accepté non transformé » ou de facture brouillon disponibles pour l'entreprise A sans créer de nouvelles données (hors périmètre autorisé). Intégré en fast-forward sur `release/commercialisation-v1`. Aucun push, aucun déploiement Production | **Risque secondaire distinct identifié, non corrigé** : `public.relances_impayes.facture_id` présente la même classe de faiblesse (clé étrangère simple vers `factures(id)`, sans lien composite avec son propre `entreprise_id`) — nécessite une autorisation séparée avant tout correctif. Voir aussi la ligne « IDOR `devis.client_id` » ci-dessous, découverte lors de la reprise de la recette |
| Recette d'écriture croisée — IDOR `devis.client_id` (découverte, correctif, validation) | **Fuite confirmée le 6 août 2026**, découverte dès la reprise de la recette d'écriture générale (arrêtée immédiatement conformément au protocole). `public.devis.client_id` n'avait jamais eu de protection contre une référence à un client d'une autre entreprise : FK simple uniquement (`devis_client_id_fkey → clients(id)`), et la policy RLS `"membres devis"` (PERMISSIVE, FOR ALL) ne contrôle que `est_membre_actif(entreprise_id)`, jamais `client_id`. Contrairement à `chantier_id` (protégé par FK composite depuis le 10 juillet 2026) et contrairement à la fuite `factures`, celle-ci était **atteignable depuis le parcours applicatif normal** : les RPC `creer_devis_brouillon`/`modifier_devis_brouillon` (`security invoker`) écrivent `client_id` sans aucune validation propre et héritent silencieusement de la lacune RLS — un appel forgé aux Server Actions `creerDevisAction`/`modifierDevisAction` (hors sélecteur UI normal, qui ne propose que les clients de l'entreprise active) aurait pu l'exploiter. Reproduite formellement dans les deux sens (INSERT A+client B, INSERT B+client A, UPDATE A→client B), transactions systématiquement annulées, marqueurs `RECETTE_IDOR_ROLLBACK_`, aucune donnée résiduelle. Cause classée **PLUSIEURS PROTECTIONS MANQUANTES** (FK composite absente et policy `WITH CHECK` incomplète). Audit des données existantes : 0 incohérence sur `client_id` et sur `chantier_id`. Point distinct documenté sans correction (hors périmètre, ne bloque pas la fiabilité de ce correctif) : 15 devis existants où le client du chantier associé diffère du client du devis, **au sein de la même entreprise** — pattern métier légitime (transfert de chantier via `chantier_transferts`), pas une incohérence inter-entreprises. **Correctif appliqué et validé** : migration `20260806000198_correctif_isolation_devis_client.sql` (clé étrangère composite `(client_id, entreprise_id) → clients(id, entreprise_id)`, réutilisant l'index unique déjà existant ; migration additive, aucune policy RLS modifiée). Validation locale : 19 assertions pgTAP dédiées + 231 tests pgTAP au total, 232 tests Vitest, `typecheck`, `lint`, `build` — tous au vert. Contrôle de cohérence Preview juste avant application : 0 incohérence. Migration appliquée sur `elsatia-preview` (`migration list` local=remote), état vivant vérifié (nouvelle contrainte + `devis_chantier_entreprise_fkey` toujours présente + 5 policies RLS inchangées). 8 tests directs sur `elsatia-preview` (transactions annulées) : les 4 opérations auparavant permissives (INSERT et UPDATE, deux sens) sont désormais bloquées (`23503`) ; les 4 opérations légitimes restent possibles, y compris un appel réel aux RPC `creer_devis_brouillon` et `modifier_devis_brouillon` avec un client de la même entreprise (non-régression applicative confirmée en conditions réelles, pas seulement par analyse de code). Après rollback : baselines reconfirmées à l'identique (devis A = 36, devis B = 1, dernier numéro A `DEV-2026-035` inchangé, 0 ligne résiduelle). Validation UI : aucune session de recette active sans transmettre de mot de passe ; limitée à l'analyse de code du sélecteur client (`src/app/(app)/devis/nouveau/page.tsx`), confirmé strictement filtré par `entreprise_id` — la navigation normale ne permet pas de sélectionner un client étranger, l'exploitation nécessitait un appel forgé, désormais bloqué en base. Intégré en fast-forward sur `release/commercialisation-v1`. Aucun push, aucun déploiement Production | Voir ligne « Audit `pieces_jointes_devis` » ci-dessous. La recette d'écriture croisée générale reste suspendue jusqu'à reprise explicite (section INSERT validée le 6 août 2026 avant suspension pour investiguer `pieces_jointes_devis` — UPDATE/DELETE/Server Actions/RPC/transitions de statut restent à reprendre) |
| Audit d'isolation — `public.pieces_jointes_devis` | **Auditée le 6 août 2026 : aucune faille exploitable confirmée.** Suspicion initiale identique à `devis.client_id` (FK simple `devis_id → devis(id)`, `entreprise_id` propre, aucune clé composite). Audit live sur `elsatia-preview` : la table n'a **qu'une seule policy RLS, en lecture** (`pieces_jointes_devis_lecture`, SELECT via `peut_lire_devis(devis_id)`) — **aucune policy PERMISSIVE ni RESTRICTIVE pour INSERT/UPDATE/DELETE**, ce qui bloque toute écriture directe via le rôle `authenticated`, y compris intra-entreprise (confirmé par test direct : `42501 new row violates row-level security policy`, identique que le `devis_id` cible soit de la même entreprise ou d'une autre). Le seul chemin d'écriture réel est la fonction `security definer` `enregistrer_pieces_jointes_devis(p_devis_id, p_pieces)` : elle relit le devis par son `id` sans filtre RLS (`security definer`), dérive `entreprise_id` **exclusivement de la ligne `devis` elle-même** (jamais d'un paramètre client), vérifie `est_membre_actif` et la permission `gerer_devis` sur cette entreprise dérivée, puis exige que `storage_path` commence par `entreprise_id/devis_id/` — rendant une incohérence structurellement impossible par construction, quel que soit l'appelant. Confirmé par appel direct de la RPC avec un `devis_id` B depuis le compte A : rejetée (`P0001 Devis inaccessible`). Aucune Server Action, aucune autre fonction SQL n'écrit dans cette table (grep exhaustif du dépôt). Aucune suppression de pièce jointe déjà attachée n'est exposée nulle part dans l'application (seul le nettoyage d'objets Storage orphelins avant finalisation existe). Audit des données existantes : 0 ligne dans la table sur Preview (aucune pièce jointe n'a jamais été créée), donc 0 incohérence par construction. Cause classée **FAIBLESSE NON EXPLOITABLE**. **Aucune migration, aucune branche, aucun correctif créés** — non justifiés au vu de l'audit. Aucune modification de code, aucune donnée, aucun objet Storage. Plan pour un futur test Storage fonctionnel (upload réel, lecture croisée, URL signée, remplacement, suppression, nettoyage) préparé mais **non exécuté** | Voir ligne « Recette d'isolation en écriture — reprise complète » ci-dessous |
| Recette d'isolation en écriture — reprise complète (INSERT/UPDATE/DELETE/Server Actions/RPC) | **Validée le 6 août 2026, sans anomalie, hors Storage et relances.** Reprise après l'audit `pieces_jointes_devis` (ligne ci-dessus), sans rejouer la section INSERT déjà validée (34/34 tests conformes le même jour). Protections précédentes reconfirmées présentes avant reprise : 7 clés étrangères composites (`devis_client_entreprise_fkey`, `devis_chantier_entreprise_fkey`, les 4 relations de `factures`, `factures_chantier_entreprise_fkey`) et les 8 policies RLS d'écriture de `chantiers`. **UPDATE** (16 tests, deux sens, transactions annulées, marqueur `RECETTE_IDOR_UPDATE_ROLLBACK`) : les tentatives sur client/chantier/salarié/devis/facture s'exécutent sans erreur SQL mais touchent **zéro ligne** (filtrage RLS silencieux du `USING`, confirmé par contrôle privilégié post-tentative) ; `depenses_fournisseurs` bloque l'UPDATE au niveau des grants (`authenticated` n'a que INSERT/SELECT sur cette table — immutable par construction, aucune RPC de modification identifiée) ; les 16 relations structurelles (devis→client/chantier, facture→client/devis/facture d'origine/facture parente, chantier→client, changement d'`entreprise_id`) restent bloquées par FK composite ou RLS (`23503`/`42501`) dans les deux sens. **DELETE** (14 tests, deux sens) : `depenses_fournisseurs` bloqué par grants (pas de DELETE) ; toutes les autres tables (client, chantier, salarié, fournisseur, devis, facture) exécutent sans erreur mais suppriment **zéro ligne** — contrôle privilégié confirmant les 7 objets de chaque entreprise toujours présents après tentative. **Server Actions** : audit de code sur `clients.ts`, `chantiers.ts`, `employes.ts`, `depenses.ts`, `devis.ts`, `factures.ts` — pattern uniforme et correct (`ctx.entrepriseId` systématiquement dérivé côté serveur de la session, jamais du client ; toute lecture/modification filtrée par `.eq("entreprise_id", ctx.entrepriseId)` avant écriture). **RPC** : 10 fonctions inventoriées côté Server Actions ; 8 examinées en détail (`creer_devis_brouillon`, `modifier_devis_brouillon`, `creer_facture_depuis_devis`, `modifier_facture_brouillon` : `security invoker`, protégées par les FK composites et/ou la RLS de l'appelant ; `dupliquer_devis` : `security invoker`, copie verbatim les colonnes déjà cohérentes de la ligne source ; `classer_facture_fournisseur`, `lier_justificatif_depense` : `security definer`, vérifient systématiquement `est_membre_actif(p_entreprise_id)` et filtrent leurs `UPDATE` par `entreprise_id = p_entreprise_id`) — aucune fonction `security definer` permettant une écriture inter-entreprises identifiée. **Transitions de statut** : `changerStatutDevisAction`/`changerStatutFactureAction`/`changerStatutChantierAction` relisent systématiquement la ressource filtrée par `ctx.entrepriseId` avant toute transition ; un UUID étranger produit un objet introuvable, transition refusée sans écriture. Baselines reconfirmées identiques après chaque groupe (A : 24/20/12/36/25/30 ; B : 1/1/1/1/1/1/1 ; dernier devis A `DEV-2026-035` inchangé) ; 0 marqueur `RECETTE_IDOR_*` résiduel à chaque contrôle. Aucune modification durable, aucun push | `pieces_jointes_devis` (protection déjà confirmée non exploitable, voir ligne ci-dessus) ; voir ligne « Correctif d'isolation `relances_impayes` » ci-dessous ; `TEST STORAGE FONCTIONNEL À AUTORISER SÉPARÉMENT` |
| Correctif d'isolation `public.relances_impayes` (découverte, correctif, validation) | **Fuite confirmée le 6 août 2026.** Contrairement à `pieces_jointes_devis` (protection totale par absence de policy d'écriture), `relances_impayes` autorise l'écriture directe sous `authenticated` : les policies `gestion_insert`/`gestion_update` ont un `WITH CHECK` réel (`a_permission(entreprise_id,'gerer_crm')`) qui protège l'appartenance de la ligne elle-même, mais ne vérifie jamais que `facture_id` (FK simple vers `factures(id)`) appartient à cette même entreprise. La seule protection réelle était applicative : la Server Action `creerRelanceAction` (`src/app/actions/suite-metier.ts`) relit la facture filtrée par `ctx.entrepriseId` avant l'insertion — contournable par un appel direct à l'API REST/SQL avec le même jeton de session. Aucune Server Action, RPC, route API ou tâche planifiée trouvée en dehors de `creerRelanceAction`/`marquerRelanceEnvoyeeAction` (toutes deux correctement scopées à `ctx.entrepriseId`, sans effet sur la faille au niveau base). Reproduite en transaction annulée (marqueur `RECETTE_IDOR_RELANCE_ROLLBACK`) : INSERT relance A + facture B → réussi ; INSERT relance B + facture A → réussi ; UPDATE d'une relance A existante vers une facture B → réussi. Seul le changement isolé d'`entreprise_id` en conservant la facture d'origine était déjà bloqué (RLS `gestion_update`). Cause classée **PLUSIEURS PROTECTIONS MANQUANTES**. Audit des données existantes : 0 ligne dans la table sur Preview, donc 0 incohérence. **Correctif appliqué et validé** : migration `20260806000199_correctif_isolation_relances_impayes.sql` (clé étrangère composite `(facture_id, entreprise_id) → factures(id, entreprise_id)`, réutilisant l'index unique déjà existant depuis le correctif `factures` ; migration additive, aucune policy RLS modifiée). Validation locale : 10 assertions pgTAP dédiées + 241 tests pgTAP au total, 232 tests Vitest, `typecheck`, `lint`, `build` — tous au vert. Contrôle de cohérence Preview juste avant application : 0 incohérence. Migration appliquée (`migration list` local=remote), état vivant vérifié (nouvelle contrainte présente, 5 policies RLS inchangées, aucune donnée modifiée). 6 tests directs sur `elsatia-preview` (transactions annulées) : les 3 opérations auparavant permissives (INSERT et UPDATE, deux sens) désormais bloquées (`23503`) ; les 2 opérations légitimes (relance de sa propre facture) restent possibles. Aucun effet externe : tous les tests sont du DML SQL brut en transaction annulée, jamais un appel à `creerRelanceAction` ni à un service d'e-mail — aucun appel réseau, aucun envoi, aucun webhook. Après rollback : baselines inchangées, 0 marqueur résiduent, table toujours à 0 ligne. Intégré en fast-forward sur `release/commercialisation-v1`. Aucun push, aucun déploiement Production | Voir ligne « Recette Storage — clôture » ci-dessous |
| Recette Storage — clôture de la recette d'isolation multi-entreprises | **Validée le 6 août 2026, sans anomalie, méthode adaptée.** Test fonctionnel réel (upload/download/URL signée via l'API HTTP Storage) impossible sans un JWT signé de session A/B réelle (mots de passe jamais détenus, jamais demandés) ni la clé service-role (usage explicitement proscrit pour démontrer la RLS) — **choix validé par l'utilisateur** : test des policies `storage.objects` directement en SQL (rôle `authenticated`, claims représentatifs, transactions systématiquement annulées), le mécanisme d'autorisation réel que l'API HTTP interroge pour chaque opération. Bucket retenu : `devis-medias` (privé, chemins `entreprise_id/devis_id/fichier`, cohérent avec le module déjà audité). Policies confirmées : `devis_medias_ajout` (INSERT, `est_membre_actif(folder[1])` + `gerer_devis`), `devis_medias_lecture` (SELECT), `devis_medias_nettoyage` (DELETE, uniquement objets orphelins sans métadonnée). Tests légitimes intra-entreprise (upload, listing) : conformes pour A et B. Tests croisés A→B et B→A sur objet réel : listing croisé → vide ; recherche du chemin exact → introuvable ; téléchargement/URL signée → non applicable (mécanisme identique au SELECT, déjà refusé) ; remplacement (UPDATE brut) → sans effet, aucune policy PERMISSIVE UPDATE n'existe pour ce bucket (la policy `role_gestion_fichiers_update` est RESTRICTIVE, son `ELSE true` ne fait qu'éviter une restriction supplémentaire sans jamais accorder l'accès seul — vérifié explicitement, non un artefact) ; upsert (mécanisme réel de remplacement, INSERT ON CONFLICT) → refusé (`42501`) ; suppression directe → refusée par Postgres lui-même indépendamment de la RLS (« Direct deletion from storage tables is not allowed ») ; création dans le dossier étranger → refusée (`42501`). Manipulation de chemin : double slash et casse modifiée de l'UUID → refusés ; UUID étranger inclus dans le nom de fichier seul (dossier légitime) → accepté sans fuite (le premier segment de chemin reste celui du propriétaire) ; séquence littérale `../` dans le nom → acceptée mais **non exploitable** (confirmé via `storage.foldername()` : le stockage objet ne traite pas `../` comme un chemin filesystem, le premier segment analysé reste celui du propriétaire légitime — faux positif de conception de test corrigé après vérification). Métadonnées `pieces_jointes_devis` (deux fixtures, une par entreprise) : lecture croisée → 0 ligne visible ; modification croisée → sans effet ; suppression croisée → **initialement mesurée à tort comme réussie** (erreur de méthode : la vérification s'exécutait sous la session de l'attaquant, qui ne peut de toute façon pas lire la ligne de la victime, donc `exists()` renvoyait faussement `false`) — corrigée par une vérification en perspective privilégiée : les deux lignes existent toujours après tentative, DELETE bien bloqué dans les deux sens. RPC `enregistrer_pieces_jointes_devis` avec un devis étranger : refusée (`P0001 Devis inaccessible`, cohérent avec l'audit initial). Aucune donnée n'a jamais persisté (toutes les opérations en transactions `ROLLBACK`) : 0 objet `RECETTE_STORAGE_*`, 0 métadonnée résiduelle, confirmé après coup. Baselines A/B inchangées, 0 marqueur `RECETTE_IDOR_*`. Aucun envoi, aucun webhook, aucun appel externe (tests strictement SQL) | Voir ligne « Régression PostgREST — embeds ambigus » puis ligne « Recette Storage — validation fonctionnelle HTTP réelle A↔B » ci-dessous |
| Régression PostgREST — embeds ambigus après clés étrangères composites (découverte, correctif, déploiement) | **Découverte le 6 août 2026**, en marge de la recette Storage HTTP, en essayant simplement de naviguer vers une fiche devis dans le cadre normal du test (« parcours applicatif normal » exigé par le protocole). Root cause : les migrations `197`/`198`/`199` (correctifs `factures`/`devis.client_id`/`relances_impayes`) ont chacune ajouté une **seconde** clé étrangère vers une même table cible (ex. `clients`) sans jamais mettre à jour les requêtes applicatives existantes qui embarquaient déjà cette relation sans la qualifier — PostgREST ne peut alors plus déterminer quelle relation utiliser et répond `400 PGRST201 "Could not embed because more than one relationship was found"`. Aggravant silencieux : la quasi-totalité des appels ne déstructurent que `{ data }` (jamais `error`), donc l'échec ne produisait aucun message d'erreur visible — seulement un `data` à `null` puis `notFound()` (404) ou un état vide, sur les pages fiches devis et factures, le tableau de bord, la facturation avancée, les impressions PDF, le CRM, les exports comptables, le copilote IA et surtout l'action serveur `creerRelanceAction` (cassée pour **toute** création de relance, tous comptes confondus, depuis l'application du premier correctif). Corrigé en qualifiant chaque embed ambigu avec la clé étrangère simple d'origine déjà utilisée pour `chantier` (`!devis_client_id_fkey`, `!factures_client_id_fkey`, etc.), sur 12 fichiers (`devis/[id]`, `factures/[id]`, `dashboard`, `facturation-avancee`, `lib/ai/copilote.ts`, `crm`, `api/exports/comptabilite`, `actions/suite-metier.ts`, `imprimer/devis/[id]`, `imprimer/factures/[id]`, `actions/paiements-en-ligne.ts`). Validation locale complète (typecheck, lint, build, 232 tests Vitest) avant fusion. Incident git worktree survenu et intégralement résolu pendant l'intégration (fichiers restaurés individuellement depuis le commit de travail via `git show`, aucune perte, vérifié par `git diff --stat` vide puis re-passage typecheck/tests). Intégré en fast-forward sur `release/commercialisation-v1` (commit `f990240 fix(postgrest): désambiguïser les embeds clients/devis/factures`). **Déployé sur Preview via `vercel deploy`** (sans `--prod`, autorisation explicite distincte), alias stable repointé (`vercel alias set`) pour préserver la continuité des sessions A/B en cours. Aucun push, aucun déploiement Production | Voir ligne « Recette Storage — validation fonctionnelle HTTP réelle A↔B » ci-dessous |
| Recette Storage — validation fonctionnelle HTTP réelle A↔B (upload/download/URL signée/routes/manipulation de chemin) | **Validée les 6 et 7 août 2026, sans anomalie.** Fait suite à la recette SQL-only (ligne ci-dessus), requalifiée par l'utilisateur comme insuffisante en l'absence d'appel HTTP réel ; exécutée avec deux sessions de navigateur authentiques (comptes A et B, mots de passe toujours saisis manuellement par l'utilisateur, jamais connus ni demandés par Claude), deux fichiers JPEG minimaux distincts générés localement (118 octets chacun, SHA-256 vérifiés : A `cd0e77a7…`, B `0a2f9613…`). Flux d'upload reproduit avec les routes applicatives réelles : `POST /api/devis/{id}/pieces-jointes/preparer` → `PUT` sur l'URL signée Storage (octets bruts) → `POST /api/devis/{id}/pieces-jointes/finaliser` (RPC `enregistrer_pieces_jointes_devis`). Uploads légitimes B puis A : réussis, intégrité confirmée par téléchargement et comparaison SHA-256 octet à octet. **Batterie croisée A→B et B→A**, symétrique et exhaustive (listing, téléchargement direct, URL signée, route applicative, remplacement, upsert, suppression, création dans le dossier étranger, appel de la RPC `enregistrer_pieces_jointes_devis` avec un devis étranger, 4 variantes de manipulation de chemin) : tous les accès croisés refusés (`400`/`403`/`404`/listing vide), résultats strictement identiques dans les deux sens et cohérents avec la recette SQL-only précédente ; la RPC sur devis étranger renvoie `403 Devis inaccessible`, cohérent avec l'audit initial de `pieces_jointes_devis`. Leçon de méthode : un jeton `Bearer` limité à la clé anonyme donne des résultats trompeurs (rôle anonyme, pas le rôle réel de l'utilisateur) — corrigé en extrayant le véritable `access_token` de session depuis le cookie `sb-*-auth-token` avant chaque test. Intégrité post-attaque vérifiée par requête SQL privilégiée : fichier de B intact après la batterie A→B, fichier de A intact (taille 118 octets, `etag` inchangé) après la batterie B→A. **Nettoyage** : aucune route de suppression n'existe nulle part dans l'application pour les métadonnées `pieces_jointes_devis` (confirmé par recherche exhaustive dans le code, y compris `DevisEditor.tsx`) — fichier de B supprimé via l'API Storage sous sa propre session réelle, après suppression préalable ciblée par SQL administratif de sa seule ligne de métadonnée (précondition exigée par la policy `devis_medias_nettoyage`, qui n'autorise la suppression d'un objet que lorsqu'aucune métadonnée ne le référence encore — mécanisme de nettoyage d'uploads orphelins, pas une policy générale de suppression par le propriétaire). Pour les deux objets fictifs restants de A (le fichier finalisé + un résidu `RECETTE_PATH_TEST4.jpg` issu de la manipulation de chemin), le mot de passe de A n'était plus disponible en session : la suppression SQL directe sur `storage.objects` a été **refusée par un trigger de protection Postgres** (« Direct deletion from storage tables is not allowed ») ; `supabase storage rm --linked --experimental` n'a supprimé aucun objet, sans erreur (cause non diagnostiquée). Une tentative de récupération de `SUPABASE_SERVICE_ROLE_KEY` via `vercel env pull` a été **immédiatement interrompue par l'utilisateur** avant toute lecture ou affichage — processus tué, fichier temporaire partiellement écrit supprimé sans jamais être lu, conformément à l'interdiction de principe de manipuler cette clé. Résolution finale : **suppression manuelle par l'utilisateur directement dans le Dashboard Supabase** (Storage → `devis-medias`), après deux tentatives infructueuses (action non réellement validée côté interface) suivies d'une troisième tentative réussie, vérifiée sans ambiguïté par requête SQL directe sur `storage.objects` (absence des deux chemins exacts, zéro objet `RECETTE_STORAGE_*` ou `RECETTE_PATH_TEST4` dans tout le bucket). Seul résidu après nettoyage : un `.emptyFolderPlaceholder`, artefact standard et bénin créé automatiquement par le Dashboard Supabase pour garder visible un dossier vidé — sans rapport avec la recette. Baselines reconfirmées strictement identiques (A : 24 clients / 20 chantiers / 12 salariés / 36 devis / 25 factures / 30 dépenses, dernier devis `DEV-2026-035`, dernière facture `FAC-2026-025` ; B : 1/1/1/1/1/1/1/1) ; statut du devis A testé (`f87905d7…`, `DEV-2026-024`) toujours `refusé`, inchangé du début à la fin (aucune action de cette recette n'a jamais touché un statut). Aucun push, aucun déploiement Production, aucune clé service-role jamais récupérée, affichée ou persistée | **Recette d'isolation multi-entreprises entièrement terminée.** Reste hors périmètre (déjà identifié séparément, non bloquant) : reprise de la section UPDATE/DELETE/Server Actions/RPC de la recette d'écriture croisée générale pour les tables restantes hors du périmètre déjà validé (voir ligne « Recette d'isolation en écriture — reprise complète » ci-dessus, qui couvre déjà l'essentiel du périmètre) |
| Checklist de lancement commercial | Document de suivi unique créé le 5 août 2026 | `docs/organisation/CHECKLIST_LANCEMENT.md` — source de référence pour l'avancement pré-lancement, à consulter avant ce registre pour tout sujet de commercialisation |
| Audit final pré-commercialisation (lecture seule) | **Réalisé le 07-08-2026**, 20 sections, sur `release/commercialisation-v1` @ `b7fd419`. Build/qualité intégralement vert (typecheck, lint, 232 Vitest, 241 pgTAP, build). Sécurité multi-tenant reconfirmée par inspection (0 table RLS désactivée, 229 fonctions `SECURITY DEFINER` toutes avec `search_path` explicite, 0 grant `anon` sur donnée métier). Constat central : socle technique sain, mais commercialisation réelle bloquée par des sujets hors code (structure juridique inexistante, aucun environnement Production distinct, aucun email transactionnel serveur). `PRODUCTION_CHECKLIST.md` identifié comme obsolète (décrit un mode prototype qui n'existe plus dans le schéma actuel). Conclusion : `AUDIT PRÉ-COMMERCIALISATION TERMINÉ — CORRECTIONS À PLANIFIER AVANT MISE EN PRODUCTION` | Voir ligne « Lot technique pré-commercialisation B3-B5 » ci-dessous |
| Lot technique pré-commercialisation B3-B5 (mot de passe, icônes PWA, js-yaml) | **Terminé et intégré le 07-08-2026** sur `release/commercialisation-v1` par fast-forward strict depuis `fix/pre-commercialisation-technique-b3-b5` (3 commits techniques + ce commit documentaire). Périmètre strictement limité aux 3 points techniques de l'audit, sans toucher à la structure juridique, à Production, à Stripe live, à Powens, à l'email transactionnel, aux documents juridiques, aux données de recette ni au nettoyage des `page 2.tsx`. **B3** : accès « Changer mon mot de passe » ajouté dans Mon espace (section « Compte et sécurité », visible dans les deux variantes de la page — avec et sans fiche employé), réutilisant tel quel `modifierMotDePasseAction`/`/nouveau-mot-de-passe` sans nouvelle implémentation ; 5 tests Vitest ajoutés (longueur minimale, confirmation différente, absence de session, succès, absence de fuite du mot de passe dans l'URL de redirection en cas d'erreur). **B4** : 4 icônes PWA générées (192/512 standard + variantes maskable avec zone de sécurité) à partir des couleurs et du wordmark officiels déjà présents dans `src/app/icon.svg` (navy `#0d1b2a`, or `#c9a24a`) via `next/og` `ImageResponse` (aucun outil externe requis) ; `manifest.ts` mis à jour ; `public/sw.js` n'a nécessité aucune modification (préfixe `/icons/` déjà générique) ; 1 test Vitest ajouté vérifiant que chaque icône déclarée existe réellement sur disque. **B5** : `js-yaml` 4.3.0 → 4.3.1 (`npm audit fix`, CVE-2026-59870), dépendance transitive d'ESLint uniquement (jamais dans le bundle runtime), diff limité à 3 lignes de `package-lock.json`, `package.json` inchangé, `npm audit --audit-level=high` → 0 vulnérabilité. Contrôles finaux après les 3 correctifs : typecheck 0 erreur, lint 0 erreur (3 warnings `<img>` préexistants inchangés, hors fichiers modifiés), **238/238 Vitest** (232 + 6 nouveaux, 52 fichiers), **241/241 pgTAP** (inchangé, aucune migration touchée), build réussi. 3 commits séparés par sujet + 1 commit documentaire. Aucune modification Supabase, aucune variable d'environnement, aucune donnée métier, aucun asset LIRIA, aucun secret. Non vérifié en direct dans un navigateur (environnement local sans configuration Supabase pour une session authentifiée) — compilation et tests unitaires confirment la correction, cohérent avec le mécanisme déjà validé en conditions réelles pendant la recette Auth | Points B1/B2 (mise à jour des checklists de lancement) et provisionnement Production restent à traiter séparément, comme prévu par l'audit |
| Lot documentaire pré-commercialisation B1-B2 (mise en cohérence des checklists) | **Terminé le 07-08-2026** sur `release/commercialisation-v1`, lecture seule côté application (aucun fichier `src/`, `supabase/`, `public/`, `package*.json` ni `.env*` touché). `PRODUCTION_CHECKLIST.md` entièrement réécrit : suppression des références au mode prototype obsolète (accès `anon` étendu, `sortie_mode_prototype.sql`, migrations « 30-37 »/« 43-44 »), structuré en A. Déjà validé sur Preview / B. Prérequis Production absents / C. Séquence de provisionnement proposée (non exécutée). `CHECKLIST_LANCEMENT.md` corrigé : les 3 cases identifiées par l'audit comme faussement décochées (authentification, isolation multi-tenant, Storage) marquées `VALIDÉ SUR PREVIEW` avec référence au registre ; lot B3-B5 consigné ; nombre de migrations obsolète (189) corrigé avec note de mise à jour (199) ; section 8 « bascule de lancement » reformulée pour refléter l'absence réelle de mode prototype plutôt que de prétendre qu'il reste à désactiver. Statuts explicites introduits dans les deux documents : `VALIDÉ SUR PREVIEW` / `À FAIRE AVANT PRODUCTION` / `À VALIDER EN PRODUCTION` / `BLOQUÉ PAR STRUCTURE JURIDIQUE` / `NON APPLICABLE`. Structure juridique, Production, Stripe live, email transactionnel et documents juridiques définitifs restent explicitement non cochés, conformément à l'audit. Aucun document `docs/juridique/*.md` modifié | Provisionnement Production à préparer séparément par un audit/prompt dédié, en lecture seule, pour déterminer précisément les projets, variables, domaines et migrations à créer sans mélanger Preview et Production |
| Audit de préparation du provisionnement Production (lecture seule) + Phase P0 | **Réalisés le 07-08-2026** sur `release/commercialisation-v1`. Audit : inventaire complet Supabase/Vercel/domaine/variables/email/Stripe/Auth/Storage/monitoring/sauvegardes, plan P0→P10, aucune écriture. Découverte notable : 35 variables documentées et utilisées par le code (OpenAI, Sentry, VAPID, Powens, `CRON_SECRET`, sécurité) absentes même de Preview — ces intégrations ne sont probablement pas fonctionnelles sur Preview aujourd'hui. Phase P0 : décisions verrouillées — Supabase/Vercel Production `elsatia-production`, région `eu-west-3` (Paris), domaine `elsatia.fr` (`www` → apex), IA/Push/Powens/Boutique/Crons désactivés explicitement au lancement (`FEATURE_AI_ENABLED=false`, `FEATURE_BOUTIQUE_ENABLED=false`, `FEATURE_CRONS_ENABLED=false` — découverte clé : ces flags sont actifs par défaut si la variable est absente, jamais désactivés implicitement), Sentry obligatoire avant premier client, fournisseur email Brevo retenu (hébergement France/Allemagne). `sortie_mode_prototype.sql` root-causé en détail : cassé (référence une fonction supprimée) et dangereusement obsolète (liste blanche RPC périmée d'environ 105 fonctions manquantes) — classé `NE PAS EXÉCUTER SUR UNE PRODUCTION VIERGE`. 193 fichiers de migration confirmés (dernière `20260806000199`), convention de comptage à retenir. 6 commits locaux synchronisés avec le remote (`git ls-remote` en repli après échec de `git fetch`, CAS A formellement vérifié, push réussi jusqu'à `68c4802`) | Voir ligne « Mini-lot garde-fou des scripts de recette Supabase » ci-dessous |
| Mini-lot garde-fou des scripts de recette Supabase | **Terminé le 07-08-2026** sur `release/commercialisation-v1` (fast-forward depuis `fix/guard-production-scripts`), lecture seule côté Supabase (aucun script exécuté, aucune donnée touchée, ni Preview ni Production). Audit direct des 7 scripts de `supabase/production/` : aucun garde de référence de projet programmatique n'existait, contrairement à `scripts/seed-elsatia-preview-year.mjs`. Mécanisme retenu : module pur testable `scripts/garde-scripts-production.mjs` (registre blanc des scripts autorisés, vérification croisée `SUPABASE_PROJECT_REF`/hôte de `NEXT_PUBLIC_SUPABASE_URL`/référence réellement liée par la CLI — trois sources indépendantes doivent concorder sur `pgvvpqyjziyapbbkydmc`, toute absence ou incohérence est un refus, jamais une autorisation par défaut) + wrapper `scripts/executer-script-production.mjs` (`node scripts/executer-script-production.mjs <fichier>`, délègue ensuite à `supabase db query --linked --file`). Script destructif `supprimer_entreprises_test.sql` : confirmation dédiée supplémentaire exigée (`CONFIRM_DELETE_TEST_DATA=YES`) ; `corriger_encodage_juju.sql` évalué comme ne nécessitant pas ce niveau renforcé (UPDATE scopé et idempotent, pas de suppression), mais reste soumis au garde de cible. `sortie_mode_prototype.sql` déplacé et renommé `supabase/production/archive/NE_PAS_EXECUTER_sortie_mode_prototype.sql`, avec en-tête d'arrêt explicite détaillant les 3 raisons (cassé, redondant, dangereux) ; retiré du registre du wrapper. Dossier `supabase/production/` conservé tel quel (nom imparfait mais renommage écarté : 8 fichiers y font référence, dont plusieurs documents d'archive `RELAIS_CHATGPT.md`/`RELAIS_CLAUDE.md` qui ne doivent pas être réécrits — un renommage aurait forcé soit à les toucher, soit à laisser des chemins incohérents dans des rapports historiques). 19 tests unitaires ajoutés (`scripts/garde-scripts-production.test.mjs`, `node --test`, aucun accès réseau), tous verts, couvrant les 6 scénarios demandés et plus. `PRODUCTION_CHECKLIST.md` et `docs/exploitation/PREVIEW_ELSATIA_CHECKLIST.md` mis à jour (chemin archivé, procédure sécurisée). `supabase/production/README.md` créé. typecheck/lint reconfirmés verts (aucun fichier `src/` touché). 2 commits séparés (technique + documentaire). Aucun script exécuté, aucun push | P1 (création du projet Supabase `elsatia-production` en `eu-west-3`, sans application des migrations) peut être préparé |
| P1 — Création du projet Supabase Production | **Terminée le 07-08-2026.** Organisation `julien gregurec` (id `grbjohkkuhexoanqgogu`) — la seule disponible, celle qui héberge déjà `elsatia-preview`. Première tentative refusée par l'API Supabase (limite de 2 projets actifs du plan gratuit déjà atteinte par l'organisation) ; aucune écriture n'en a résulté. Sur autorisation explicite de l'utilisateur, l'ancien projet inutilisé `uykukebthgmqtxlmkpnn` (« julien-gregurec's Project », `eu-west-1`, créé le 10-07-2026, jamais lié à ce dépôt — reliquat de la toute première phase prototype, antérieure à `elsatia-preview`) a été supprimé pour libérer un créneau ; `elsatia-preview` non affecté (reconfirmé `ACTIVE_HEALTHY`, `linked: true` après coup). Nouvelle tentative réussie : projet **`elsatia-production`** créé, référence **`exhvuzegsefmoguxoiak`**, région **`eu-west-3` (Paris)**, statut `ACTIVE_HEALTHY`, PostgreSQL 17.6.1.155 (identique à Preview), créé le 2026-08-07T23:09:11Z. Mot de passe de base de données généré aléatoirement au moment de la création, jamais affiché, jamais écrit dans un fichier ni transmis dans la conversation. **Aucune clé service-role récupérée.** Projet volontairement **non lié** au worktree local (`supabase link` non exécuté — le lien CLI reste sur `elsatia-preview`, `linked: false` confirmé pour `elsatia-production`) ; aucune migration appliquée ; aucun bucket créé manuellement ; aucune donnée, aucun utilisateur Auth, aucune entreprise de recette. État Auth/Storage/extensions détaillé non vérifié en direct (nécessiterait de lier le dépôt ou d'utiliser la clé service-role, tous deux hors périmètre de P1) — le projet est dans l'état par défaut standard d'un projet Supabase fraîchement créé. Coût : la création elle-même n'a entraîné aucun changement de plan constaté (réussie une fois le créneau libéré, sans invite de mise à niveau) | P2 (application des 193 migrations sur `elsatia-production`) à autoriser séparément, avec vérification explicite de la référence cible avant toute commande d'écriture |
| P2 — Bootstrap Supabase Production par migrations | **Terminée le 07-08-2026** dans un worktree dédié `/Users/juliengregurec/Projects/elsatia-production-bootstrap` (branche `ops/production-bootstrap`, base `977a558`), strictement isolé du worktree Preview `liria-codex` (resté lié à `pgvvpqyjziyapbbkydmc` tout du long, reconfirmé après coup). Worktree Production lié via `supabase link --project-ref exhvuzegsefmoguxoiak` (`supabase/.temp/project-ref` confirmé égal à cette référence, jamais `pgvvpqyjziyapbbkydmc`). Audit préalable : base parfaitement vierge confirmée (0 table publique, `supabase_migrations.schema_migrations` inexistante, 0 bucket). Aperçu (`--dry-run`) puis application réelle des **193 fichiers de migration officiels** de `supabase/migrations/` via `supabase db push --linked` (sans `--include-seed`, `seeds:[]` confirmé) : les 193 se sont appliqués sans erreur (`Finished supabase db push.`). Un avertissement Docker sans rapport (cache local du catalogue de schéma) est apparu, sans conséquence sur le push réel. Vérification post-migration : 193/193 local=remote, dernière `20260806000199`. Structure identique à Preview : 145 tables publiques, 429 policies RLS, 91 triggers, mêmes 5 extensions (`pg_stat_statements`, `pgcrypto`, `plpgsql`, `supabase_vault`, `uuid-ossp`). Sécurité confirmée : 0 table avec RLS désactivé, 0 fonction `SECURITY DEFINER` avec `EXECUTE` accordé à `PUBLIC`, les 7 contraintes composites correctives présentes (`devis_chantier_entreprise_fkey`, `devis_client_entreprise_fkey`, `factures_client_entreprise_fkey`, `factures_devis_origine_entreprise_fkey`, `factures_facture_origine_entreprise_fkey`, `factures_facture_parente_entreprise_fkey`, `relances_impayes_facture_entreprise_fkey`), policies RLS d'écriture `chantiers` présentes (correctif 196), grants `anon` limités aux 4 mêmes tables catalogue en lecture seule que sur Preview (`catalogue_options_abonnement`, `catalogue_services_mise_en_service`, `modeles_roles_predefinis`, `plans_abonnement`). 12 buckets Storage créés (`entreprise-assets` public, 11 privés), 0 objet Storage. Données système présentes (8 `plans_abonnement`, 9 `modeles_roles_predefinis`, 99 `permissions_disponibles`, 8 `catalogue_options_abonnement`, 6 `catalogue_services_mise_en_service`) ; **zéro donnée métier** (0 entreprise, client, chantier, employé, devis, facture, dépense), **zéro marqueur `RECETTE_*`/`DEMO-18M`/`juju`**, **0 utilisateur Auth**. Aucun seed, aucun script de `supabase/production/` exécuté. Aucune configuration Auth/SMTP/domaine effectuée (état par défaut d'un projet neuf). Aucun secret révélé. Commit documentaire créé sur `ops/production-bootstrap` uniquement, non fusionné dans `release/commercialisation-v1`, aucun push | Fusion de `ops/production-bootstrap` et P3 (Vercel Production, Auth, Brevo, domaine, Sentry, Stripe) à autoriser séparément |
| Sécurité multi-entreprises et RLS | Phase 1 terminée localement | Rapport enregistré par le commit `e9a996c` sur `release/commercialisation-v1` |
| Intégrations fournisseurs PunchOut / EDI | Architecture prête, développement non commencé | `docs/architecture/integrations-fournisseurs.md` |
| Outil de recherche de nom et de marque | Centralisé, autonome | `tools/naming-studio/` |
| Problème IA avec Anthropic | Idée/problème enregistré, diagnostic à faire | Créer une tâche avec l’erreur exacte et le scénario de reproduction |
| Documentation officielle | À structurer progressivement | Master Plan, documentation développeur/API/IA, manuels, installation et changelog |
| Vidéo officielle | En attente des variantes de production validées | Ne pas régénérer les anciens médias pendant le lot 6 |
| Vision et business | Marque « Elsatia » déposée à l'INPI le 01-08-2026 (n° 5284384) ; structure juridique toujours en backlog | Voir `docs/organisation/CHECKLIST_LANCEMENT.md` section 3 et 3bis |
| ELSATIA Colors | Backlog produit séparé | Fonctionnalités, développement et stratégie à cadrer avant implémentation |

## Règles simples pour la suite

1. Consulter ce registre avant de démarrer un nouveau lot.
2. Mettre à jour le tableau lorsqu’un lot est validé ou change d’état.
3. Utiliser des branches et worktrees Git dédiés sans mélanger leurs modifications.
4. Ne commencer le lot 7 qu’après une autorisation explicite.
5. Ne pousser, déployer ou appliquer une migration en production qu’après une autorisation distincte.
