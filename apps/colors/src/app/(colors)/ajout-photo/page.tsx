import Link from "next/link";import { getContexteColors } from "@/lib/contexte";import { resoudreRoleColors } from "@/lib/acces-colors";import { peutEffectuerColors } from "@/lib/permissions-colors";
export default async function AjoutPhotoPage(){const c=await getContexteColors();const r=await resoudreRoleColors(c);return <><header className="page-heading"><div><span className="eyebrow">Saisie terrain</span><h1>Ajout par photo</h1><p>Créez d’abord le seau, puis photographiez-le depuis sa fiche. L’OCR reste optionnel et toute suggestion devra être confirmée.</p></div></header><article className="panel photo-first"><span className="step">1</span><h2>Créer la fiche minimale</h2><p>Marque, produit, quantité et emplacement suffisent pour commencer.</p>{peutEffectuerColors(r,"ajouter_seau")?<Link className="primary-action" href="/inventaire">Créer un seau</Link>:<span className="badge">Votre rôle est en lecture seule</span>}</article><article className="panel photo-first"><span className="step">2</span><h2>Prendre la photo</h2><p>Formats JPEG, PNG, WebP ou HEIC, 10 Mo maximum. Le fichier reste privé et rattaché à votre organisation.</p></article><article className="panel photo-first"><span className="step">3</span><h2>Confirmer les données</h2><p>Le contrat de fournisseur OCR est prêt, sans appel payant ni reconnaissance active dans ce jalon.</p></article></>}
