// Colors currently uses standard CSS — no Tailwind, no PostCSS plugins.
// If Tailwind (or any other plugin) is adopted, declare it explicitly here
// AND in apps/colors/package.json.
const config = {
  plugins: {},
};

export default config;
