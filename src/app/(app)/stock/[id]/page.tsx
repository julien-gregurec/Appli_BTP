import Link from"next/link";import{notFound}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{getContexteEntreprise}from"@/lib/entreprise";import{ajouterTeinteAction}from"@/app/actions/stock";import{euros}from"@/lib/devis";
export default async function ArticleStockPage({params,searchParams}:{params:Promise<{id:string}>,searchParams:Promise<{error?:string;success?:string}>}){const{id}=await params,msg=await searchParams,ctx=await getContexteEntreprise(),sb=await createClient();const[{data:a},{data:teintes},{data:mouvements}]=await Promise.all([sb.from("articles_stock").select("*").eq("id",id).eq("entreprise_id",ctx.entrepriseId).maybeSingle(),sb.from("article_teintes").select("*").eq("article_id",id).eq("entreprise_id",ctx.entrepriseId).eq("actif",true).order("nom"),sb.from("mouvements_stock").select("id,date,type,quantite,motif,teinte:article_teintes(nom)").eq("article_id",id).eq("entreprise_id",ctx.entrepriseId).order("created_at",{ascending:false}).limit(50)]);if(!a)notFound();const action=ajouterTeinteAction.bind(null,id),un=<T,>(v:T|T[]|null):T|null=>Array.isArray(v)?v[0]??null:v;return <main className="p-8"><div className="mx-auto max-w-4xl space-y-6"><div><Link href="/stock" className="text-sm text-neutral-500 hover:underline">← Stock</Link><h1 className="mt-1 text-xl font-semibold">{a.reference} · {a.designation}</h1><p className="text-sm text-neutral-500">{a.marque??"Sans marque"} · code-barres {a.code_barres??"non renseigné"}</p></div>{msg.error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{msg.error}</p>}{msg.success&&<p className="rounded bg-green-50 p-3 text-sm text-green-700">{msg.success}</p>}<div className="grid grid-cols-3 gap-3"><div className="rounded border p-4"><p className="text-xs text-neutral-500">Stock</p><strong className="text-xl">{a.quantite_stock} {a.unite}</strong></div><div className="rounded border p-4"><p className="text-xs text-neutral-500">Seuil</p><strong className="text-xl">{a.seuil_alerte} {a.unite}</strong></div><div className="rounded border p-4"><p className="text-xs text-neutral-500">Prix achat HT</p><strong className="text-xl">{euros(a.prix_achat_ht)}</strong></div></div><section className="rounded border p-4"><h2 className="font-semibold">Nuancier / teintes disponibles</h2><div className="mt-3 flex flex-wrap gap-2">{teintes?.map(t=><div key={t.id} className="flex items-center gap-2 rounded-full border px-3 py-1.5 text-sm"><span className="h-5 w-5 rounded-full border" style={{background:t.code_hex??"#e5e5e5"}}/><span>{t.nom}</span>{t.reference&&<span className="font-mono text-xs text-neutral-400">{t.reference}</span>}</div>)}{!teintes?.length&&<span className="text-sm text-neutral-500">Aucune teinte.</span>}</div><form action={action} className="mt-4 grid grid-cols-4 gap-2 border-t pt-4"><input name="nom" required placeholder="Nom de la teinte" className="rounded border px-3 py-2 text-sm"/><input name="reference" placeholder="Référence fabricant" className="rounded border px-3 py-2 text-sm"/><input name="code_hex" type="color" defaultValue="#c9a24a" className="h-10 w-full rounded border"/><button className="rounded bg-neutral-900 px-3 py-2 text-sm text-white">Ajouter la teinte</button></form></section><section><h2 className="mb-2 font-semibold">Mouvements</h2><div className="rounded border">{mouvements?.map(m=>{const t=un(m.teinte as {nom:string}|{nom:string}[]|null);return <div key={m.id} className="grid grid-cols-[110px_130px_100px_1fr] border-t px-3 py-2 text-sm first:border-0"><span>{m.date}</span><span>{m.type.replace("_"," ")}</span><strong>{m.quantite} {a.unite}</strong><span className="text-neutral-500">{t?`${t.nom} · `:""}{m.motif??""}</span></div>})}{!mouvements?.length&&<p className="p-6 text-center text-sm text-neutral-500">Aucun mouvement.</p>}</div></section></div></main>}
