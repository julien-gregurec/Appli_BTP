import Link from"next/link";import{notFound}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{getContexteEntreprise}from"@/lib/entreprise";import{ajouterReglementDepenseAction}from"@/app/actions/depenses";import{DEPENSE_CATEGORIES,DEPENSE_STATUTS}from"@/lib/depenses";import{euros}from"@/lib/devis";
const un=<T,>(v:T|T[]|null):T|null=>Array.isArray(v)?v[0]??null:v;
export default async function DepensePage({params,searchParams}:{params:Promise<{id:string}>,searchParams:Promise<{error?:string;success?:string}>}){const{id}=await params;const msg=await searchParams;const ctx=await getContexteEntreprise();const sb=await createClient();const[{data:d},{data:r}]=await Promise.all([sb.from("depenses_fournisseurs").select("*,fournisseur:fournisseurs(nom),chantier:chantiers(id,nom),commande:commandes_fournisseurs(id,numero)").eq("id",id).eq("entreprise_id",ctx.entrepriseId).maybeSingle(),sb.from("reglements_fournisseurs").select("*").eq("depense_id",id).eq("entreprise_id",ctx.entrepriseId).order("date",{ascending:false})]);if(!d)notFound();const f=un(d.fournisseur as {nom:string}|{nom:string}[]|null),c=un(d.chantier as {id:string;nom:string}|{id:string;nom:string}[]|null),cmd=un(d.commande as {id:string;numero:string}|{id:string;numero:string}[]|null),st=DEPENSE_STATUTS[d.statut],reste=Math.max(0,Number(d.montant_ttc)-Number(d.montant_regle)),action=ajouterReglementDepenseAction.bind(null,id);return <main className="p-8"><div className="mx-auto max-w-3xl space-y-6"><div><Link href="/depenses" className="text-sm text-neutral-500 hover:underline">← Dépenses</Link><h1 className="mt-1 text-xl font-semibold">{d.numero_piece}</h1><p className="text-sm text-neutral-500">{f?.nom} · {DEPENSE_CATEGORIES[d.categorie]}</p></div>{msg.error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{msg.error}</p>}{msg.success&&<p className="rounded bg-green-50 p-3 text-sm text-green-700">{msg.success}</p>}<div className="grid grid-cols-3 gap-3"><div className="rounded border p-3">TTC<strong className="block">{euros(d.montant_ttc)}</strong></div><div className="rounded border p-3">Réglé<strong className="block text-green-700">{euros(d.montant_regle)}</strong></div><div className="rounded border p-3">Reste<strong className="block text-amber-700">{euros(reste)}</strong></div></div><section className="rounded border p-4 text-sm dark:border-neutral-800"><p><b>Statut :</b> <span style={{color:st?.couleur}}>{st?.label}</span></p><p><b>Date :</b> {d.date_piece} · <b>Échéance :</b> {d.date_echeance??"—"}</p><p><b>HT :</b> {euros(d.montant_ht)} · <b>TVA :</b> {euros(d.montant_tva)}</p>{c&&<p><b>Chantier :</b> <Link href={`/chantiers/${c.id}`} className="hover:underline">{c.nom}</Link></p>}{cmd&&<p><b>Commande :</b> <Link href={`/commandes/${cmd.id}`} className="hover:underline">{cmd.numero}</Link></p>}</section>{reste>0&&!['annulee','litige'].includes(d.statut)&&<form action={action} className="grid grid-cols-4 gap-2 rounded border p-4 dark:border-neutral-800"><input name="montant" type="number" min="0.01" max={reste} step="0.01" defaultValue={reste} required className="rounded border px-3 py-2 text-sm dark:bg-neutral-900"/><input name="date" type="date" defaultValue={new Date().toISOString().slice(0,10)} required className="rounded border px-3 py-2 text-sm dark:bg-neutral-900"/><select name="mode" className="rounded border px-3 py-2 text-sm dark:bg-neutral-900"><option value="virement">Virement</option><option value="prelevement">Prélèvement</option><option value="cb">Carte</option><option value="cheque">Chèque</option><option value="especes">Espèces</option></select><input name="reference" placeholder="Référence" className="rounded border px-3 py-2 text-sm dark:bg-neutral-900"/><button className="col-span-4 rounded bg-neutral-900 px-4 py-2 text-sm text-white dark:bg-white dark:text-neutral-900">Enregistrer le règlement</button></form>}<section><h2 className="mb-2 font-semibold">Règlements</h2>{r?.map(x=><div key={x.id} className="flex justify-between border-t py-2 text-sm"><span>{x.date} · {x.mode}</span><span>{x.reference??""}</span><strong>{euros(x.montant)}</strong></div>)}{(!r||!r.length)&&<p className="text-sm text-neutral-500">Aucun règlement.</p>}</section></div></main>}
