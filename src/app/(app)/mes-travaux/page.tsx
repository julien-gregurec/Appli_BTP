import {createClient} from "@/lib/supabase/server";import {getContexteEntreprise} from "@/lib/entreprise";
type Ligne={designation:string;description:string|null;quantite:number;unite:string};type D={id:string;numero:string|null;statut:string;date_emission:string;chantier_id:string;chantier:string;notes:string|null;lignes:Ligne[]};
export default async function MesTravauxPage(){const ctx=await getContexteEntreprise();const sb=await createClient();const{data,error}=await sb.rpc("mes_devis_chantiers_sans_prix",{p_entreprise_id:ctx.entrepriseId});const devis=(data??[]) as D[];return <main className="p-8"><div className="mx-auto max-w-5xl space-y-6"><div><h1 className="text-xl font-semibold">Mes travaux à réaliser</h1><p className="text-sm text-neutral-500">Prestations des devis acceptés ou envoyés sur vos chantiers. Les prix et totaux ne sont jamais transmis ici.</p></div>{error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{error.message}</p>}{devis.map(d=><article key={d.id} className="overflow-hidden rounded-lg border"><div className="bg-[#0d1b2a] p-4 text-white"><div className="flex justify-between gap-3"><div><h2 className="font-semibold">{d.chantier}</h2><p className="text-xs text-white/60">{d.numero??"Devis"} · {d.date_emission}</p></div><span className="rounded-full bg-white/10 px-3 py-1 text-xs">{d.statut}</span></div></div><div className="divide-y">{d.lignes.map((l,i)=><div key={`${l.designation}-${i}`} className="p-4"><div className="flex justify-between gap-3"><strong>{l.designation}</strong><span className="font-mono text-sm">{l.quantite} {l.unite}</span></div>{l.description&&<p className="mt-1 whitespace-pre-wrap text-sm text-neutral-600">{l.description}</p>}</div>)}</div>{d.notes&&<p className="border-t bg-neutral-50 p-4 text-sm"><strong>Consignes :</strong> {d.notes}</p>}</article>)}{!devis.length&&!error&&<p className="rounded border border-dashed p-8 text-center text-sm text-neutral-500">Aucun devis de chantier ne vous est encore affecté.</p>}</div></main>}
