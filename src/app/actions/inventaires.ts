"use server";import{redirect}from"next/navigation";import{revalidatePath}from"next/cache";import{createClient}from"@/lib/supabase/server";import{getContexteEntreprise}from"@/lib/entreprise";
const ch=(f:FormData,n:string)=>String(f.get(n)??"").trim();
export async function creerInventaireAction(f:FormData){const ctx=await getContexteEntreprise();const sb=await createClient();const{data,error}=await sb.rpc("creer_inventaire_stock",{p_entreprise_id:ctx.entrepriseId,p_zone_id:ch(f,"zone_id")||null,p_commentaire:ch(f,"commentaire")||null});if(error||!data)redirect(`/inventaires?error=${encodeURIComponent(error?.message??"Erreur")}`);revalidatePath("/inventaires");redirect(`/inventaires/${data}`)}
export async function enregistrerInventaireAction(id:string,f:FormData){const ctx=await getContexteEntreprise();const sb=await createClient();const{data:lignes}=await sb.from("lignes_inventaire").select("id").eq("inventaire_id",id).eq("entreprise_id",ctx.entrepriseId);if(!lignes)redirect(`/inventaires/${id}?error=${encodeURIComponent("Inventaire introuvable")}`);const comptages=lignes.map(l=>({ligne_id:l.id,quantite:Number(ch(f,`q_${l.id}`))}));const valider=ch(f,"intention")==="valider";const{error}=await sb.rpc("enregistrer_comptage_inventaire",{p_entreprise_id:ctx.entrepriseId,p_inventaire_id:id,p_comptages:comptages,p_valider:valider});if(error)redirect(`/inventaires/${id}?error=${encodeURIComponent(error.message)}`);revalidatePath("/inventaires");revalidatePath(`/inventaires/${id}`);revalidatePath("/stock");redirect(`/inventaires/${id}?success=${encodeURIComponent(valider?"Inventaire validé et stock ajusté":"Comptage enregistré")}`)}
