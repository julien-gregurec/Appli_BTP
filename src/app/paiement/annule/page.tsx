import Link from "next/link";
import { createClient } from "@/lib/supabase/server";

const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export default async function PaiementAnnulePage({searchParams}:{searchParams:Promise<{facture_id?:string}>}){const{facture_id}=await searchParams,{data:{user}}=await(await createClient()).auth.getUser(),factureId=facture_id&&UUID.test(facture_id)?facture_id:null,cible=user&&factureId?`/factures/${factureId}`:"/";return <main className="grid min-h-screen place-items-center bg-neutral-50 p-6"><div className="max-w-md rounded-2xl border bg-white p-8 text-center shadow-sm"><h1 className="text-xl font-semibold">Paiement non finalisé</h1><p className="mt-2 text-sm text-neutral-600">Aucun règlement n’a été enregistré. Vous pouvez reprendre le paiement depuis le lien reçu.</p><Link href={cible} className="mt-6 inline-flex rounded-md bg-[#0d1b2a] px-4 py-2 text-sm font-semibold text-white">{user&&factureId?"Retour à la facture":"Retour à Liria Gestion Pro"}</Link></div></main>}
